name: chit_chat
description: A Demo Chat Application on the @protocol.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  at_chat_flutter: ^2.0.1
  at_contacts_flutter: ^2.0.1
  at_onboarding_flutter: ^2.1.2
  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
